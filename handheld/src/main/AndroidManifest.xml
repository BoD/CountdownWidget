<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="org.jraf.android.countdownwidget"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="MissingVersion">

    <!-- android:sharedUserId="org.jraf.android" -->

    <application
        android:name="org.jraf.android.countdownwidget.handheld.app.Application"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name">

        <!-- Meta data for Google Play Services -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- AppWidget -->
        <receiver
            android:name="org.jraf.android.countdownwidget.app.appwidget.AppWidgetProvider"
            android:label="@string/appwidget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/appwidget" />
        </receiver>

        <!-- Settings -->
        <activity
            android:name="org.jraf.android.countdownwidget.handheld.app.settings.SettingsActivity"
            android:icon="@drawable/ic_launcher">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>